# Карта: Реестр ников чатерсов

## Destination

Реестр ников чатерсов канала живёт в репо (`content/chatters.toml`): идемпотентный синк ников из чата VOD, правило для агента — резолвить написание ника и дописывать алиасы, и починенные написания ников в существующих карточках.

## Notes

- Термины — из `CONTEXT.md` («Чатерс», «Лот», «Привет»); избегать «зритель», «юзер», «мем-лот».
- Источники: `docs/agents/twitch-sources.md` (архив чата VOD), `docs/agents/media-pipeline.md` + `content/lots.toml` (карточки).
- Эффорт несёт исполнение: destination — изменение в репо, а не только решения.
- Скоуп — только авторинг карточек агентом. Рантайм приложения — вне карты.

## Decisions so far

- [Идентичность ника и актуальный способ скачать чат VOD](issues/01-nick-identity-and-chat-source.md): канон — display-имя (`nick`), ключ дедупа — `login` (или числовой user id); оба способа скачивания живы, рекомендован `TwitchDownloaderCLI`.
- [Реестр `content/chatters.toml` и синк из чата](issues/02-chatters-file-and-sync.md): 172 ника из VOD `2864275043`; `scripts/chatters_sync.py` + `just chatters-sync` (idempotent по `id`/`login`, `aliases` не трогает).
- [Правило для агента и указатели](issues/03-agent-rule-and-pointers.md): правило резолва ника — в `docs/agents/chatters.md`, указатель в `AGENTS.md`, шаг в `media-pipeline.md`.

## Not yet specified

<!-- туман: в скоупе, но ещё не нарезать -->

- Не-чаттерсы в карточках: `bradhi`, «Аврора», «Монах», «Салат» — как резолвить, если ника нет в чате VOD.
- Валидация: должна ли проверка связывать ники в карточках с реестром (рецепт `just` или хук).
- Расширение реестра полями (частота сообщений, VOD-источник), если понадобится позже.

## Out of scope

- Рантайм приложения: таблица в Supabase, UI-автокомплит/поиск, RLS — реестр нужен только агенту при авторинге.
- Коммит сырых `chat.json` в репо — сырьё эфемерно, коммитим только реестр.
