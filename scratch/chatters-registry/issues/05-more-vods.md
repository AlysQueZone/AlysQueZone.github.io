# Догрузить реестр со свежих VOD

Status: open
Type: task
Blocked by: none

## Question

Расширить покрытие реестра: прогнать синк на нескольких свежих VOD канала, не только на `2864275043`.

- Взять 2–3 свежих VOD (ID — из `twitch.tv/videos/<id>`; VOD живут 14–60 дней). Один прогон умеет много: `just chatters-sync --vod A --vod B`.
- Прогон идемпотентный: новые ники добавятся, у существующих подтянется актуальный `nick`/`id`, `aliases` не тронутся.
- Проверить, не прилетели ли боты; при необходимости расширить `EXCLUDE` в `scripts/chatters_sync.py`.

Ответ: сколько VOD прогнано, +N новых ников, у скольких существующих изменился `nick`.
