# 16: Гамба-фаусет: таблица F, все выигрыши в плюс

**What to build:** Гамба становится щедрым аттракционом вместо казино с edge: новая таблица выплат (ставка 100): 30% — мимо (0), 40% — +50 (150), 22% — +200 (300), 8% — джекпот x10 (1000). Возврата «в ноль» больше нет — любой невыигрышный исход кроме мимо даёт плюс. Со 100 ставки возвращается в среднем 206, 7 спинов из 10 в плюсе. Это осознанный пересмотр решений 01/04/05 (там RTP 90% и sink 10%): синков в экономике не остаётся, балансы и цены будут расти — цена веселья на шуточных деньгах.

**Blocked by:** None (can start immediately).

**Status:** done (миграция `supabase/migrations/20260908300000_gamba_faucet_table_f.sql` — CHECK-superset + DELETE+INSERT seed F; RPC не тронут, конфиг из БД; клиент `src/lib/gamba.ts` + `src/components/GambaModal.astro` — 4 исхода, колесо 50 сегментов 15/20/11/4; `spec.md` — таблица F, ~206 со 100, синков нет; симуляция: EV 206.0, MC 1M 206.00; `npm run build` зелёный)

- [x] Новая миграция обновляет seed таблицы выплат (append-only, старые строки не правим); RPC считает по новому конфигу без смены кода (конфиг из БД)
- [x] Колесо и плакат показывают 4 исхода с новыми шансами/номиналами (мимо/+50/+200/x10); шансы видны до спина как раньше
- [x] Спека `spec.md` обновлена: числа таблицы, «со 100 возвращается ~206», sink'ов нет (одна-две строки, не переписывать)
- [x] Симуляция подтверждает средние ~206 со 100 (повторить приёмку тикета 12 на новой таблице)
- [x] На прод не накатывать; `npm run build` зелёный
