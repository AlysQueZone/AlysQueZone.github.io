# 11: Живая цена перекупа и похороны localStorage

**What to build:** «Забрать за N» везде берётся из подписки на БД (кнопка перекупа, колокол, карточка), открытая модалка live-обновляет цену, клиентская формула next-цены удалена из обоих мест. Контрактфаза: локальный кошелёк удаляется — не осталось ни одного читателя localStorage-баланса, межвкладочный `storage`-синк убран.

**Blocked by:** 08 (те же триггеры и модалка покупки — сначала деньги, потом живость цены).

**Status:** done (миграция `supabase/migrations/20260908260000_lot_next_price.sql` — вью `lots_with_next_price` security_invoker + гранты + страховка публикации lots; клиент `src/lib/prices.ts` + живые N в модалке/колоколе/витрине; `wallet.ts` удалён, гость видит прочерк; `npm run build` зелёный)

- [x] Кнопка перекупа, колокол и карточка показывают N из подписки; перед записью — свежий select, итог — цена сервера
- [x] Цена ушла при открытой модалке — новая цена видна до нажатия, отдельного ретрая нет
- [x] Уведомление о перекупе: факт уплаченной цены + живая кнопка возврата; при нехватке — уведомление в тоне канала
- [x] Локального баланса не осталось: поиск по ключу хранилища пуст, `npm run build` зелёный
