# 10: Ежедневный вход по календарю

**What to build:** В шапке рядом с балансом живёт Ежедневный вход: первый клик дня начисляет награду по календарю 100/100/150/150/200/250/500, пропуск дня streak не сбрасывает, повторный клейм в тот же день — тихо ничего не делает.

**Blocked by:** 07 (нужен счёт и баланс).

**Status:** done (миграция `supabase/migrations/20260908250000_daily_login_claims.sql`, клиент `src/lib/daily.ts` + `src/components/DailyLogin.astro`, точка входа в шапке рядом с балансом; `npm run build` зелёный)

- [x] Клейм начисляет сумму дня streak сервером и пишет движение в леджер
- [x] Повторный клейм в тот же день награду не дублирует; пропуск дня streak не сбрасывает, день 7 даёт 500
- [x] Гость без сессии забрать вход не может — уводится во вход с возвратом к месту
- [x] `npm run build` зелёный; RLS: чужие клеймы недоступны
