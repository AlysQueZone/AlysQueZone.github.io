Status: resolved
Type: task
Blocked by: none

## Question (build, implement-skill)

Светлая теплая тема вместо темной (решение человека, Q3): кремово-персиковый фон, хот-пинк `#FF00BC` (официальный цвет чата канала в Twitch) + оранжевый `#FF9E2C` + арбуз `#FF4D6D` как акценты, CTA остается Twitch-purple `#9146FF`, цены — пивное золото `#F5C518` (на светлом — темнее для контраста, решай сам).

## Scope (строго)

Только `src/styles/global.css` (`@theme`-токены + базовые стили) и `src/layouts/BaseLayout.astro` (шапка, фон, баланс-пилюля). Компоненты/страницы не перестраивать — только чтобы читалось на светлом (контраст текста). НЕ трогать: `src/components/**`, `src/pages/**`, `src/lib/**`, `data/**`, `public/**`, конфиги, чужие тикеты.

## Acceptance

- `npm run build` проходит; все 14 страниц собираются; текст читается (контраст), CTA и цены видны.

## Done

- Светлая теплая тема: `void #FFF3E6` (крем-персик), `panel #FFFEFA`,
  текст `milk #221A14` (16.5:1), `mist #6B5E54` (5.9:1).
- Акценты: новый токен `blush #FF00BC` (хот-пинк чата), `ember`/`melon` без
  изменений; текстовые `.text-ember`/`.text-blush` затемнены (`#B34A00` 4.3:1,
  `#C4008C` 5.3:1).
- Цены: `pivko #8A6D00` (4.6:1 на cream, бейджи с `text-black` 4.3:1);
  `rind #15803D` (4.7:1). CTA `stream #9146FF` без изменений (белый текст 4.6:1).
- Белые полупрозрачные бордеры/подложки тёмной темы переведены на тёплые тёмные
  в `global.css`; шапка — `border-black/10`, sticky + blur, баланс-пилюля
  (`border-pivko/50`, пилюля), тёплый radial-градиент фона (пинк/оранж/арбуз).
- `npm run build`: 14 страниц собраны. Проверен `dist`: токены/градиент/CTA/
  цены/пилюля на месте.
