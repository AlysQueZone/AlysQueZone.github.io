Status: resolved

## Task

Кнопка «Получить еще пивкойнов» теперь НАЧИСЛЯЕТ +1000 (решение человека): клик → звук `m4-scum.mp3` → модалка «Но это не точно — держи еще 1000 пивкойнов» → баланс +1000 в `localStorage`, пилюля баланса обновляется.

## Scope (строго)

`BaseLayout.astro` (шапка/кнопка/модалка) + `src/lib/wallet.ts` (добавить `earn(1000)` если нет; механизм обновления пилюли — как при покупке). Звук и confetti/тост не ломать. НЕ трогать: лоты, тему, остальные страницы/компоненты, конфиги, деплой, чужие тикеты.

## Acceptance

- `npm run build` проходит; клик factual +1000 к балансу, переживает reload; повторные клики работают.

## Done

- `src/lib/wallet.ts`: добавлен `earn(amount)` — начисляет в `localStorage` (`BALANCE_KEY`), возвращает новый баланс; невалидные/неположительные суммы игнорирует.
- `src/layouts/BaseLayout.astro`: клик по scum-кнопке теперь `earn(1000)` + `render()` пилюли (тот же механизм, что при покупке), звук `m4-scum.mp3` и открытие модалки не тронуты; текст модалки — «Но это не точно — держи еще 1000 пивкойнов».
- Проверка: `npm run build` проходит; `earn` код-инспекцией + node-прогоном с моком localStorage: 1000→2000→3000 (дельты +1000), значение переживает «reload» (перечитывание из store), повторные клики суммируются.
