Status: resolved

## Task

Баг светлой темы (репорт человека): тост «Не хватает Пивкойнов, пойжем жоско на стрим» — черный текст на черном, не видно совсем.

## Scope (строго)

Компонент тоста (+ заодно проверить модалку покупки/обманки: фоны/тексты на светлой теме, тот же класс багов чинить сразу). Цвета — только через `@theme`-токены с достаточным контрастом на светлом фоне. НЕ трогать: логику покупки/кошелька, лоты, остальные страницы, конфиги, деплой, чужие тикеты.

## Acceptance

- `npm run build` проходит; все тосты/модалки читаются на светлой теме (проверить все варианты: успех, нехватка, обманка).

## Done

- Тост `#buy-toast` в `src/components/BuyModal.astro`: `bg-black` без цвета текста → `bg-panel text-milk border-2 shadow-lg` (только @theme-токены). Контраст текста 1.23:1 → 16.97:1.
- Все 3 варианта тоста (успех `border-rind`, нехватка и обманка `border-melon`) читаются на светлой теме.
- Модалки проверены: buy-modal и scum-modal уже на `bg-panel` (текст 16.97:1, вторичный 6.2:1, CTA 4.64:1) — тот же баг отсутствует, не трогал.
- `npm run build` — проходит (21 страница).
