# 09: Вход через Twitch + сессия в шапке

**What to build:** Чатерс входит через Twitch одной кнопкой в шапке сайта и остаётся залогиненным между визитами; выход — одной кнопкой. Незалогиненного кнопка «Купить» ведёт в логин с возвратом к тому же Лоту. Провайдер и redirect-адреса уже настроены человеком в дашборде на этапе прототипа — тикет только про код сайта.

**Blocked by:** None (can start immediately).

**Status:** done (subagent реализовал, проверено: review diff + grep секретов + `npm run build` зелёный, 23 стр.)

- [x] Кнопка «Войти через Twitch» в шапке; после логина там же логин и кнопка «Выйти»

- [ ] Кнопка «Войти через Twitch» в шапке; после логина там же логин и кнопка «Выйти»
- [ ] Сессия долгая (persist + авторефреш), переживает перезагрузку страницы
- [ ] «Купить» без сессии уводит в логин и возвращает к тому же Лоту с переоткрытой модалкой
- [ ] Вход проверен на dev-адресе и прод-адресе (оба в белом списке redirect URL)
