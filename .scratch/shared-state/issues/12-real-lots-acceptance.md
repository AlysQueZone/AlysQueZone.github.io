# 12: Реальные Лоты и финальная приёмка

**What to build:** Биржа запускается на настоящем ассортименте: seed прототипа заменён реальными Лотами каталога, throwaway-прототип удалён, прод-приёмка пройдена.

**Blocked by:** 10, 11.

**Status:** done (код сабагента проверен: сид 20 реальных лотов + ЛУК locked, throwaway удалён, `npm run build` зелёный; живая приёмка — за человеком, см. ниже)

## Comments

- 2026-09-07: миграция сида применена человеком через SQL Editor (CLI `db push` упирался в отказ 5432 с локальной сети — лечится пулером 6543, но Editor сработал). Dev-сервер слушает только IPv6-localhost: открывать `http://localhost:4321`, не `127.0.0.1`.

- [ ] Seed `proto-*` заменён реальными Лотами (названия, стартовые цены и владельцы из каталога)
- [ ] Throwaway-прототип из трекера удалён, в прод не утекло
- [ ] Прогнаны Database Advisors без ошибок, RLS как в спеке
- [ ] Двухбраузерный чеклист зелёный + сайт и логин открываются из РФ без VPN + `npm run build` зелёный
