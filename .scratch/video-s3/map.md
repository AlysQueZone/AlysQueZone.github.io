## Destination

Спека переезда медиа на видео готова к стройке: мем-приветы — инлайн-видео в карточке (старые 9 лотов — хотлинк на файлы MemeAlerts, ручной маппинг человеком; будущие свои — Supabase Storage), UI-звуки — публичный Storage-бакет, `data/lots.json` переезжает в БД. Готово = все тикеты закрыты, спека передается в имплементацию без новых решений; код в этой карте не трогаем.

## Notes

- Домен: шуточная Биржа приветов канала alysque. Глоссарий — `CONTEXT.md` (Привет, Лот, Мем-лот, Владелец, Чатерс, Пивкойн, Биржа, Мои приветы, Пауза). Внимание: канон «Мем-лот — лот со звуком и постером» устареет в ходе карты (станет видео); править `CONTEXT.md` только на тикете модели данных после решения с человеком. Ход по `docs/agents/issue-tracker.md` (Wayfinding operations).
- Skills для сессий: `grilling` + `domain-modeling` для решений с человеком; `research` для AFK-фактов (Supabase Storage, MemeAlerts); `prototype` для дешёвого инлайн-видео. При любой работе с Supabase обязательно `supabase` + `supabase-postgres-best-practices` (схема только миграциями, Deploy to production вкл — мерж в `main` сам применяет миграции).
- Standing preferences: своего сервера нет; 0₽/мес; всё открывается из РФ (GitHub Pages — эталон); статика Astro; хотлинк на чужие CDN только для уже разрешённого (7TV/Twitch/Static-CDN) + теперь MemeAlerts-файлы для старых лотов; бинарники не коммитить; `privets/` — gitignored, оригиналы кладёт человек.
- Решения гриллинга 2026-09-08 (раунды 1–2): конец = спека для стройки; видео ЗАМЕНЯЕТ связку аудио+постер (одно поле); старые лоты — хотлинк на «их s3», маппинг вручную человеком; UI-звуки (clap, gamba, outbid) — на наш S3; инлайн-видео прямо в карточке; источник правды — БД (`lots.json` больше не нужен); бакет — один публичный (`sounds/` + место под свои видео).
- Трекер: local-markdown, эта карта — `.scratch/video-s3/map.md`, тикеты — `.scratch/video-s3/issues/NN-<slug>.md`.

## Decisions so far

<!-- пусто: charting-сессия ничего не резолвит -->

## Not yet specified

- Момент удаления `public/sounds/` + `public/memes/` из репо и сборки (до/после заливки в Storage, редиректы старых путей).
- Фолбэк, если файл MemeAlerts умер/сменил URL: постер-заглушка, hide-карточки, копия к себе как план Б.
- Порядок деплоя: миграция колонок → заливка звуков → правка фронта → маппинг URL (что блочит релиз).
- Автоплей-политики (muted inline vs click-to-play) и трафик на мобильных — детали после прототипа.
- LUK-легенда: нужно ли ей видео-поле вообще.

## Out of scope

- Новые карточки из `privets/` (axel, bradhi_pizdota, Revixit, spiritbanbanban, VandalQuE) — отдельная задача; здесь только «подготовить поле» (куда лить, какой формат, как потом добавить лот).
- Сам код/заливка/маппинг (только решения + спека; имплементация — следующим усилием).
- Свой сервер/VPS/докер/прокси и платные тарифы — запрещено условиями.
- Полноценный auth кроме Twitch, платежи, корзина, личные кабинеты, мобильное приложение, кастомный домен.
