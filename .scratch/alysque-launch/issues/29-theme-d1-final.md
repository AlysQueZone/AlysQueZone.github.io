Status: resolved

## Task

Перенос финальной схемы D1 из `.scratch/alysque-launch/prototype/colors2.html` в прод + правка человека: фон и карточки должны различаться сильнее (сейчас почти один цвет).

## Значения (решение человека + моя детализация, не менять)

- Фон `#FFF6BF`, бар `#FFE4A6`, карточки `#FFE6AC` + бордер `#F0C46C` (ступень глубже фона — видно, но уютно).
- CTA маджента-пинк `#C2187B` (hover `#9E145F`), якорь `#FF00BC`, детали `#92400E`.
- Фиолетовый запрещен везде: выгрести `#9146FF` и оттенки из всех компонентов (grep), заменить на мадженту/пинк по смыслу.
- Бейджи: rare — терракот, legendary — маджента (как в D1). Текст контраст ≥4.5:1.

## Scope (строго)

`src/styles/global.css` (токены), места использования фиолетового (grep `#9146FF`/purple/stream — компоненты, layout, страницы), карточка (фон+бордер). Логику, лоты, звуки, данные не трогать. НЕ трогать: конфиги, деплой, чужие тикеты.

## Acceptance

- `npm run build` проходит; grep по `#9146FF`/purple в `src/` пуст; карточки визуально отделены от фона (фон/бордер на месте в `dist`).

## Done

- Токены D1 в `src/styles/global.css`: фон `#FFF6BF`, бар `#FFE4A6`, карточки `#FFE6AC` + бордер `#F0C46C`, CTA `#C2187B`/hover `#9E145F`, якорь `#FF00BC`, детали `#92400E`, бейджи rare `#9A3412`/legendary `#B3127A` + кремовый текст `#FFFBEA`.
- Фиолетовый вычищен из `src/` (токен, тень чемодана, confetti, CTA-х ховеры `hover:bg-ctahover`); grep `#9146FF`/purple пуст. Остаток `#9146FF` только в `public/favicon.svg` (вне скоупа, не трогал).
- Карточки переведены на `bg-panel`/`border-card` (лот-карты, топ, «как работает», страница лота, hero).
- `npm run build` проходит; в `dist` CSS на месте `fff6bf`/`ffe6ac`/`f0c46c`/`c2187b`/`9e145f`.
