Status: resolved

## Task

Баг из приемки тикета 16: в общем дереве `npm run build` падает из-за лишнего `</div>` в `src/layouts/BaseLayout.astro` (строки ~36–37, коммит тикета 15). Убрать лишний тег минимумом, ничего больше не менять.

## Acceptance

- `npm run build` проходит на общем дереве, 14 страниц.

## Done

- Проверка общего дерева (HEAD = коммит тикета 15): лишнего `</div>`
  в `src/layouts/BaseLayout.astro` нет — 3 `<div` / 3 `</div>`, дерево
  сбалансировано (фон, шапка-контейнер, пилюля баланса).
- По правилу «минимумом, ничего больше не менять» layout не тронут.
- `npm run build`: проходит, 14 страниц собраны.
