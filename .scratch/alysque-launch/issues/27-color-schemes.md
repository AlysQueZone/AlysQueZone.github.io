Status: resolved
Type: prototype
Blocked by: none

## Question (prototype, skill `prototype`, ветка UI)

Цветовые схемы сайта: сейчас тема — гибрид с остатками темной. Показать разные варианты реализации, чтобы человек выбрал.

## Жесткие рамки (решение человека, не обсуждаются)

1. Только светлая тема (темных фонов/блоков нет вообще).
2. Только теплые, уютные цвета.
3. Фиолетовый запрещен (`#9146FF` и близкие — нигде: ни CTA, ни ссылки, ни акценты).
4. Основные цвета: желтый, оранжевый, розовый (хот-пинк `#FF00BC` — якорь).

## Scope (строго)

Throwaway-файл `.scratch/alysque-launch/prototype/colors.html` (как `birzha-prototype.html`: дабл-клик, ноль зависимостей). 3 структурно одинаковых, но палитрно разных варианта через `?variant=A/B/C` + плавающий свитчер (стрелки, `←/→`, URL шарится) + показ текущего состояния. На странице — репрезентативный набор: шапка с балансом, hero, карточка лота (владелец, цена, бейдж редкости), CTA-кнопка, модалка покупки, тост. Данные — 3–4 лота инлайн (скопировать из `data/lots.json`, тянуть JSON не надо).

Варианты (все светлые/теплые/без фиолетового, различаются настроением):
- A «Крем + хот-пинк»: кремовый фон, пинк — главный акцент/CTA.
- B «Персик + оранжевый»: персиковый фон, оранжевый CTA, розовый — вторичный.
- C «Солнечный»: светло-желтый фон, кораллово-розовый CTA, оранжевые детали.

Контраст текста везде ≥ 4.5:1 (баг тоста из 24 не повторять). Прод не трогать, в `src/` ничего не класть.

## Acceptance

- Файл открывается дабл-кликом; все 3 варианта листаются; ни одного фиолетового пикселя (проверить grep по `#9146FF`/purple в файле); человек выбирает победителя.

## Done

Файл: `.scratch/alysque-launch/prototype/colors.html`. Grep по `#9146FF`/purple/violet/indigo — чисто; контрасты текста/CTA/тоста ≥4.5:1 проверены расчетом.
