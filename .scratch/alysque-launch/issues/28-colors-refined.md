Status: resolved
Type: prototype
Blocked by: none

## Question (prototype, skill `prototype`, ветка UI)

Второй заход цветовой схемы: человек выбрал вариант C («Солнечный») из `colors.html`, но с правками. Показать refined-схему(ы) для финального выбора.

## Жесткие рамки (решение человека)

1. Только светлая, только теплая, фиолетовый запрещен (как раньше).
2. Меньше белого: верхний бар НЕ белый (теплый тинт), фон карточек НЕ белый (теплый тинт).
3. Палитра — по приложенным 7 полосам (слева направо): бледно-желтый `#FFF6BF` → светлый персик `#FFE4A6` → мягкий оранжевый `#FFC878` → коралл `#FF9E66` → лососево-розовый `#FF7FA8` → розовый `#F75C9C` → маджента-пинк `#E91E8C` (хот-пинк `#FF00BC` — якорь, рядом с последней полосой). Допустимы соседние оттенки ±шаг, холодных/фиолетовых нет.
4. Красным кнопкам — нет: CTA только пинк/маджента или глубокий оранжевый (не кораллово-красный).

## Scope (строго)

Throwaway-файл `.scratch/alysque-launch/prototype/colors2.html` (дабл-клик, ноль зависимостей). Два подварианта через `?variant=D1/D2` + плавающий свитчер как в `colors.html`: D1 — CTA маджента-пинк, D2 — CTA глубокий оранжевый; остальное одинаково (фон бледно-желто-кремовый, бар персиковый, карточки кремово-персиковые, цены/бейджи — теплые оттенки шкалы). Тот же набор элементов: шапка с балансом, hero, карточка лота, CTA, модалка, тост; 3–4 лота инлайн. Контраст текста ≥4.5:1. Прод (`src/`, деплой) не трогать.

## Acceptance

- Файл открывается дабл-кликом; D1/D2 листаются; белого (`#fff`, `#ffffff`, white) в фонах нет — проверить grep; красного CTA нет; человек выбирает финал.

## Done

Файл: `.scratch/alysque-launch/prototype/colors2.html` (D1 CTA маджента `#C2187B`, D2 CTA глубокий оранжевый `#9A3412`; фон `#FFF6BF`, бар `#FFE4A6`, карточки `#FFEFC0`; grep-контроль пройден, контраст ≥4.5:1).
