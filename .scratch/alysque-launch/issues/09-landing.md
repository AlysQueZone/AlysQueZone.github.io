Status: resolved
Blocked by: 08

## Task

Лендинг `src/pages/index.astro` по спеке (вариант A прототипа): hero («БИРЖА ПРИВЕТОВ. СТРИМ ВКЛЮЧИЛСЯ, А ТЫ?» + чемодан «продам привет», CTA `#9146FF`), «как это работает» (3 шага), топ приветов (первые 3–4 лота из `data/lots.json`), CTA на `/lots/`. Тон канала, мемы из тикета 02, дисклеймера нет.

## Acceptance

- `npm run build` проходит, секция hero/топ рендерятся из `lots.json` (не хардкод в разметке).
- Мобильная ширина не разваливается.

## Done

- Создан `src/pages/index.astro`: hero («БИРЖА ПРИВЕТОВ. СТРИМ ВКЛЮЧИЛСЯ, А ТЫ?» + чемодан «продам привет»,
  CTA `bg-stream` → `{base}lots/`), «как это работает» (3 шага), топ приветов (первые 4 лота из
  `data/lots.json` через `lots.slice(0, 4)` — рендер из JSON, не хардкод), финальный CTA на биржу.
  Тон канала («пойжем жоско», «жду тебя 🙂🙂🙂»), дисклеймера нет.
- `npm run build` проходит (1 page, `/index.html`), все 12 проверок `dist/index.html` — OK.
  Мобильная ширина: `flex-col → md:row`, сетка `grid-cols-1 → sm:2 → lg:4`, `min-w-0`/`break-words`/`truncate`.
- Затронуты только свои файлы: `src/pages/index.astro` + этот тикет.
