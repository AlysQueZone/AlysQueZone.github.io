Status: resolved

## Task

Кнопка «Получить еще пивкойнов» → «гамба на пивкойны» с рулеткой (решение человека).

## Поведение

- Кнопка в шапке: больше размером, надпись «гамба на пивкойны».
- Клик → модалка-рулетка: колесо-canvas из 10 сегментов (5× «+1000», 4× «мимо», 1× «+100000» — веса 50/40/10 видны сразу), крутка ≤5с с ease-out, щелчки барабана (синтезировать WebAudio — короткий click с замедляющимся интервалом, без файлов), фоном играет мем-звук.
- Исход — взвешенный рандом ровно по шансам; в конце: фон стоп, звук исхода, начисление (`earn`, уже есть в wallet) или ничего, текст результата в модалке/тосте в тоне канала.
- Исходы и звуки (mp3 извлечь из webm ниже через `ffmpeg -i in.webm -vn -codec:a libmp3lame -q:a 5 out.mp3`, закоммитить в `public/sounds/`):
  - +1000, 50%: `https://cdns.memealerts.com/p/64ff3737bbee8620b9c91311/a4acccd7-59a0-4027-9ff7-46927f63658f/alert_orig.webm` → `gamba-win1k.mp3` (~22КБ).
  - мимо, 40%: существующий `public/sounds/m4-scum.mp3` (переиспользовать).
  - +100000, 10%: `https://cdns.memealerts.com/p/65f9c787288f43c13cbe1299/a7a09b1f-4656-46f9-9d17-fc93d2acb6b0/alert_orig.webm` → `gamba-win100k.mp3` (~124КБ).
  - фон крутки: `https://cdns.memealerts.com/p/6483188d2017f179dedcb9a5/a38c5b7d-f658-4402-a5a5-0e8f69bdd32a/alert_orig.webm` → `gamba-bg.mp3` (~100КБ).
- Рулетку писать свою легкую (canvas ~60 строк), готовые либы не тянуть. Автоплей ок (всё по клику), graceful-fallback без звука.

## Scope (строго)

Новая модалка-рулетка (компонент + клиентский скрипт), правка кнопки в шапке, 3 mp3, `earn` переиспользовать. НЕ трогать: стили тоста нехватки (тикет 31 — параллельный агент), тему, лоты, конфиги, деплой, чужие тикеты.

## Acceptance

- `npm run build` проходит; шансы 50/40/10 (исход взвешен, не «первый попавшийся сегмент»); крутка ≤5с; баланс меняется только на выигрышах; звуки играют и не накладываются друг на друга (фон стопается).

## Done

- Кнопка в шапке (`src/layouts/BaseLayout.astro`): «гамба на пивкойны», крупнее (`px-5 py-2 text-sm`), открывает рулетку; старый scum-флоу тикета 22 (бесплатные +1000 по клику) удалён вместе с его модалкой/скриптом.
- Новая `src/components/GambaModal.astro`: canvas-колесо 10 сегментов (5×+1000, 4×мимо, 1×+100000, веса в подписи), крутка 4.2с ease-out, WebAudio-щелчки на границах сегментов (редеют сами), фон `gamba-bg.mp3` (loop, стоп в конце), исход взвешен 50/40/10 до анимации, `earn` только на выигрышах, результат в модалке в тоне канала, graceful-fallback без звука.
- Звуки: `public/sounds/gamba-win1k.mp3` (21КБ), `gamba-win100k.mp3` (124КБ), `gamba-bg.mp3` (100КБ); проигрыш переиспользует `m4-scum.mp3`.
- `npm run build` проходит (21 страница). `BuyModal.astro` не тронут (параллельный тикет 31).
