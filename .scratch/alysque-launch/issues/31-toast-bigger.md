Status: resolved

## Task

Тост нехватки «Не хватает Пивкойнов, пойжем жоско на стрим» сделать заметно больше (решение человека): сейчас слишком мелкий.

## Scope (строго)

Только стили тоста (размер шрифта/паддинги) в компоненте тоста. Тексты, логику, остальные тосты/модалки не менять (только если тот же размерный класс переиспользуется — тогда ок). НЕ трогать: шапку/кнопку гамбы (тикет 32), логику, конфиги, деплой, чужие тикеты.

## Acceptance

- `npm run build` проходит; тост нехватки крупный и читаемый.

## Done

- `src/components/BuyModal.astro` (#buy-toast): `text-sm` → `text-xl`, `px-5 py-2` → `px-8 py-4`. Тексты/логика не тронуты. `npm run build` — ок.
