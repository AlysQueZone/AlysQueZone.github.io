Status: resolved

## Task

Фон-арт F1 в прод (выбор человека): качественный широкий арт вместо сплошного фона, под кремовой вуалью как в прототипе F1.

## Факты разведки (готово, не делать заново)

- Сжатый webp уже лежит в `/tmp/bg-full.webp` (1920px, 113КБ, из исходника 1672x941 2.1МБ): скопировать в `public/bg/sunflowers.webp` (создать папку), проверить размер.
- Исходник `ChatGPT Image Sep 4, 2026, 11_44_35 PM.png` (2.1МБ) в корне репо — после проверки webp УДАЛИТЬ из репо (человек просил сжать предварительно, оригинал у него).
- Референс: `.scratch/alysque-launch/prototype/bg.html?variant=F1` (cover, center, вуаль ~0.88, текст поверх читаем).

## Scope (строго)

`public/bg/sunflowers.webp` (новый), фон в `BaseLayout` (fixed cover center + кремовая вуаль поверх, контент поверх; градиентную подложку можно убрать/оставить как подложку под картинкой — решай сам, дешевле). Контраст текста ≥4.5:1 сохранить. НЕ трогать: лоты, логику, звуки, конфиги, деплой, чужие тикеты.

## Acceptance

- `npm run build` проходит; в `dist` webp на месте и подключен как фон; корневой PNG удален; текст читаем.

## Done

- `public/bg/sunflowers.webp` — копия `/tmp/bg-full.webp` (113448 байт, WebP 1920x1081).
- `src/layouts/BaseLayout.astro` — фон F1: слой webp (fixed inset-0, cover/center/no-repeat, `-z-20`) + кремовая вуаль `rgba(255,246,191,0.88)` (`-z-10`); градиентную подложку убрал (разрешено скоупом), `bg-void` остался как фолбэк; цвета текста не тронуты.
- Корневой `ChatGPT Image Sep 4, 2026, 11_44_35 PM.png` (2.1МБ, untracked) удалён с диска.
- `npm run build` — OK (23 страницы); в `dist/bg/sunflowers.webp` на месте, фон подключён в `dist/index.html`.
